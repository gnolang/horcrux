name: Tests

on:
  workflow_dispatch:
  pull_request:
  push:
    branches:
      - main
    paths:
      - '**.go'
      - '**.mod'
      - '**.sum'
      - '**.proto'
      - 'Makefile'

jobs:
  unit:
    runs-on: ubuntu-latest
    steps:
      # Install and setup go
      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          # Manifest-available base; go.mod's toolchain directive upgrades it via GOTOOLCHAIN=auto.
          go-version: '1.25.x'

      # checkout horcrux
      - name: checkout horcrux
        uses: actions/checkout@v4

      # run tests
      - name: run horcrux tests
        run: make test
  e2e:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        test:
          - TestMultipleChainHorcrux
          - TestChainPureHorcrux
          - TestDownedSigners2of3
          - TestDownedSigners3of5
          - TestLeaderElection2of3
          - Test2Of3SignerThreeSentries
          - Test2Of3SignerThreeSentriesUniqueConnection
          - TestUpgradeValidatorToHorcrux
          - TestSingleSignerTwoSentries
          - TestHorcruxProxyGRPC
    steps:
      # Install and setup go
      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          # Manifest-available base; go.mod's toolchain directive upgrades it via GOTOOLCHAIN=auto.
          go-version: '1.25.x'

      # checkout horcrux
      - name: checkout horcrux
        uses: actions/checkout@v4

      # run test matrix
      - name: run test
        run: cd test && go test -v -timeout 30m -run ^${{ matrix.test }}$ .
